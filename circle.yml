machine:
  node:
    version: 6.3.0

deployment:
  qa:
    branch: master
    commands:
      - make deploy
  int:
    tag: /int-v*/
    commands:
      - make deploy
  canary:
    tag: /canary-v*/
    commands:
      - make deploy
  production:
    tag: /prod-v*/
    commands:
      - make deploy
