machine:
  node:
    version: 6.3.0

deployment:
  master:
    branch: master
    commands:
      - make deploy
  int:
    tag: /qa-v.*/
    commands:
      - make deploy
  canary:
    tag: /canary-v.*/
    commands:
      - make deploy
  production:
    tag: /prod-v.*/
    commands:
      - make deploy
