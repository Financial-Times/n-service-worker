machine:
  node:
    version: 6.3.0

dependencies:
  cache_directories:
    - "bower_components"

deployment:
  production:
    tag: /v[0-9]+(\.[0-9]+)*/
    owner: Financial-Times
    commands:
      - make deploy
