machine:
  node:
    version: 6.11.0
deployment:
  master:
    branch: master
    commands:
      - make deploy-by-day
  int:
    tag: /qa-v.*/
    commands:
      - make deploy
  canary:
    tag: /canary-v.*/
    commands:
      - make deploy
  production:
    tag: /prod-v.*/
    commands:
      - make deploy
